export type TasksType = {
    tasks?: any;
    setTasks?: any;
}

export type TaskType = {
    id?: string;
    title?: string;
    completed?: boolean;
}

export type ThemeType = {
    theme?: "dark" | "light";
    setTheme?: any;
}